sentence = "Якщо я скажу, як все почалося, ти здивуєшся."  # будь-яке речення

# Приводимо речення до нижнього регістру, щоб знайти і «як», і «Як»
sentence_lower = sentence.lower()

# Розбиваємо речення на слова
words = sentence_lower.split()

# Проходимо по кожному слову і перевіряємо, чи починається воно на 'як'
for word in words:
    # Прибираємо розділові знаки, щоб не заважали перевірці
    clean_word = word.strip(".,!?;:-()«»\"'")
    if clean_word.startswith("як"):
        print(clean_word)
